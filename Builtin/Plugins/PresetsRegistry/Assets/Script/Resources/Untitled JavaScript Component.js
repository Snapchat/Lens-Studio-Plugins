// @component

// Code placed here runs on awake, before any events fire.

// Uncomment the events below as needed:

// script.createEvent('OnStartEvent').bind(() => {
//     // Runs once before this component's first frame.
// });

// script.createEvent('UpdateEvent').bind(() => {
//     // Runs every frame while this component is active.
// });
