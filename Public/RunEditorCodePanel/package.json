{
  "name": "run-editor-code-panel",
  "version": "5.18.0",
  "files": [
    "module.json",
    "dist",
    "README.md"
  ],
  "private": true,
  "scripts": {
    "build:plugin": "tsc",
    "build:ui": "vite build",
    "prebuild": "mkdir -p ui/types && cp ../../shared-types/editor.d.ts ui/types/editor.d.ts && cp ../../shared-types/editor.d.ts ui/types/editor.txt",
    "build": "npm run build:ui && npm run build:plugin",
    "clean": "rm -rf dist",
    "watch": "tsc -w",
    "prepack": "npm run clean && npm run build",
    "pack": "npm-pack-zip --add-version"
  },
  "devDependencies": {
    "monaco-editor": "^0.52.2",
    "npm-pack-zip": "^1.3.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.1"
  }
}
