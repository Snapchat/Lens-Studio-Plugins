{
  "name": "specs-asset-jobs",
  "type": "module",
  "main": "dist/main.js",
  "version": "5.24.0",
  "private": true,
  "description": "Lens Studio-owned background SPECS text-to-3D and image-to-3D jobs with late barrier collection.",
  "scripts": {
    "clean": "rm -rf dist dist-test",
    "test:compile": "tsc -p tsconfig.test.json",
    "test": "npm run test:compile && node dist-test/tests/coordinator.test.js && node dist-test/tests/recovery.test.js && node dist-test/tests/remoteProtocol.test.js && node dist-test/tests/contracts.test.js",
    "typecheck": "tsc --noEmit",
    "buildPlugin": "tsc",
    "build": "npm run clean && npm run buildPlugin",
    "export": "mkdir -p build/resources/$npm_package_version && zip -r build/resources/$npm_package_version/package.zip dist module.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
